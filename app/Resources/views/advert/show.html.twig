{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Annonce {{ advert.id }}</h1>

    <table class='table table-striped'>
        <tbody>

            <tr>
                <th>Date</th>
                <td>{% if advert.date %}{{ advert.date|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ advert.title }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ advert.author }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ advert.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a class='btn btn-primary' href="{{ path('platform_advert_index') }}">retour à la liste list</a>
        </li>
        <li>
            <a class='btn btn-primary' href="{{ path('platform_application_new', { 'id': advert.id }) }}">Envoyer sa candidature</a>
        </li>
        <li>
            <a class='btn btn-primary' href="{{ path('platform_advert_edit', { 'id': advert.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
